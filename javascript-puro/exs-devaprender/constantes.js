/* Uma váriavel constante não pode ter seu valor redefinido, como podemos ver no exemplo abaixo.
Sempre use essa váriavel quando se tem valores que não precisem ser redefinidos.

Já o 'let' use quando precisar estar redefinindo valores.
 */
const resultadoSorteio = 12;
// resultadoSorteio = 25;
console.log(resultadoSorteio)